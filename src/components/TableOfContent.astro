---
import type { MarkdownHeading } from "astro";

interface Props {
  headings: MarkdownHeading[];
}

const { headings } = Astro.props;
---

<nav>
  <ul>
    {
      headings.map((heading) => {
        return (
          <li class="text-lg mb-4 text-secondary-500">
            <a href={`#${heading.slug}`}>#{heading.text}</a>
          </li>
        );
      })
    }
  </ul>
</nav>
